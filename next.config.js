/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configuração padrão: Server-side rendering e SSG
  reactStrictMode: true,
  images: {
    unoptimized: true
  }
};

module.exports = nextConfig; 